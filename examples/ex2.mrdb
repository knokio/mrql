-- select mean(age) as res from stuff 

def_query media_stuff():
   table = stuff

   map(row):
      emit({"count":1,"sum":row.age})

   reduce(a,b):
      return({"sum":a.sum + b.sum, "count":a.count + b.count})

   finalize(final_row):
     return {"res": final_row.sum / final_row.count}
